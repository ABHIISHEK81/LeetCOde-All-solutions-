// 190. Reverse Bits  //




class Solution(object):
    def reverseBits(self, n):
        """
        :type n: int
        :rtype: int
        """
        bits=bin(n)[2:]
        bits="0"*(32-len(bits))+bits;
        return int(bits[::-1],2)
